<assembly>
    <id>resources</id>
    <formats>
        <format>zip</format>
    </formats>
    <fileSets>
        <fileSet>
            <!-- Take everything inside the root directory -->
            <directory></directory>
            <!-- Place the zip inside the unit directory in target -->
            <outputDirectory>/</outputDirectory>
            <!-- Exclude files and directories -->
            <excludes>
                <exclude>pom.xml</exclude>
                <exclude>*.iml</exclude>
                <exclude>*.jar</exclude>
                <exclude>**/src/**</exclude>
                <exclude>**/target/**</exclude>
                <exclude>resource.xml</exclude>
            </excludes>
        </fileSet>
    </fileSets>
</assembly>